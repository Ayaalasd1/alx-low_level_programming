#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The charactervto print
 *
 * Return: On success 1.
 * On error, -1 is returned, and ernno is set appropriately.
 */
int _puthar(char c)
{
return (write91, &c, 1)
}
